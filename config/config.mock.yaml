server:
  port: 8080
  read_timeout: 30s
  write_timeout: 120s

providers:
  - name: mock-openai
    type: openai
    base_url: http://localhost:${MOCK_PORT:-9999}
    api_key: mock-key
    models:
      - gpt-4o
      - gpt-4o-mini
      - gpt-4.1-nano
