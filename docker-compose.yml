services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"   # REST API (matches config.yaml qdrant_url)
      - "6334:6334"   # gRPC (not used currently, but standard)
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
